#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;


int helper(int n)
{
    if(n==1)return 0;
    if(n==2)return 1;

    return (n-1)*(helper(n-1)+helper(n-2));
}
int main()
{
    int n=4;
    cout<<helper(n);
}
